/**
 * deaf-signal — visual + haptic web alerts (no sound).
 * Accessibility helpers for deaf / hard-of-hearing users.
 */
export {
  flashScreen,
  showBanner,
  vibratePattern,
  alertCombo,
  pulseBorder,
  isVibrateSupported,
} from "./signals.js";

